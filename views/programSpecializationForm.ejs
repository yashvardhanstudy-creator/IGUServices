<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="shortcut icon" href="https://igu1.ucanapply.com/images/webicon.png" type="image/png">
    <meta charset="UTF-8">
    <title><%= locals.program ? "Edit" : "New" %> Program & Specialization</title>
    <link rel="stylesheet" href="/stylesheets/index.css">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f7f6;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }
        .container {
            background: white;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            max-width: 500px;
            width: 100%;
        }
        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #555;
        }
        input, select {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }
        input[readonly] {
            background-color: #e9ecef;
            cursor: not-allowed;
        }
        button {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
        }
        .back-btn {
            background: #6c757d;
            margin-top: 10px;
        }
        .back-btn:hover {
            box-shadow: 0 5px 20px rgba(108, 117, 125, 0.4);
        }
    </style>
</head>

<body>
    <div class="container">
        <h1><%= locals.program ? "Edit" : "New" %> Program & Specialization</h1>
        <form action="/program-specialization" method="POST">
            <label for="program_code">Program Code (Unique):</label>
            <input type="text" id="program_code" name="program_code" required 
                   value="<%= locals.program ? locals.program.program_code : '' %>" 
                   <%= locals.program ? 'readonly' : '' %>
                   placeholder="e.g. BTECH-CSE-2024">

            <label for="subject_name">Subject Name (Title for Full Syllabus):</label>
            <input type="text" id="subject_name" name="subject_name" required 
                   value="<%= locals.program ? locals.program.subject_name : '' %>" 
                   placeholder="e.g. Master of Science">

            <label for="specialization">Specialization (Optional):</label>
            <input type="text" id="specialization" name="specialization" 
                   value="<%= locals.program ? locals.program.specialization : '' %>" 
                   placeholder="e.g. Computer Science (Leave blank for 'None')">

            <label for="level">Level (UG / PG):</label>
            <select id="level" name="level" required onchange="updateSchemes()">
                <option value="">Select Level</option>
                <option value="UG" <%= locals.program && locals.program.level === 'UG' ? 'selected' : '' %>>Undergraduate (UG)</option>
                <option value="PG" <%= locals.program && locals.program.level === 'PG' ? 'selected' : '' %>>Postgraduate (PG)</option>
            </select>

            <label for="scheme">Scheme:</label>
            <select id="scheme" name="scheme" required>
                <option value="">Select Scheme</option>
            </select>

            <label for="num_pos">Number of POs (8 to 12):</label>
            <input type="number" id="num_pos" name="num_pos" required min="8" max="12"
                   value="<%= locals.program ? locals.program.num_pos : '11' %>">

            <label for="num_peos">Number of PEOs (4 or 5):</label>
            <input type="number" id="num_peos" name="num_peos" required min="4" max="5"
                   value="<%= locals.program ? locals.program.num_peos : '4' %>">

            <label for="num_psos">Number of PSOs (0 to 4):</label>
            <input type="number" id="num_psos" name="num_psos" required min="0" max="4"
                   value="<%= locals.program ? locals.program.num_psos : '2' %>">

            <button type="submit">Save Program Data</button>
        </form>
        <button type="button" class="back-btn" onclick="window.location.href='/'">Back to Home</button>
    </div>

    <script>
        const existingScheme = "<%= locals.program ? locals.program.scheme : '' %>";

        function updateSchemes() {
            const level = document.getElementById('level').value;
            const schemeSelect = document.getElementById('scheme');
            
            schemeSelect.innerHTML = '<option value="">Select Scheme</option>';
            
            let options = [];
            if (level === 'UG') {
                options = ['A', 'B', 'C', 'D'];
            } else if (level === 'PG') {
                options = ['P', 'Q', 'R', 'S'];
            }
            
            options.forEach(opt => {
                const isSelected = existingScheme === opt ? 'selected' : '';
                schemeSelect.innerHTML += `<option value="${opt}" ${isSelected}>Scheme ${opt}</option>`;
            });
        }
        window.onload = updateSchemes;
    </script>
</body>
</html>
